server.port: 8091

spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        greetings-in:
          destination: greetings1
          contentType: application/json
        greetings-out:
          destination: greetings2
          contentType: application/json